import { describe, it, expect } from 'vitest'

// This file ensures all test files are discovered by Vitest
// The actual tests are in their respective files

describe('Test Suite Index', () => {
    it('should have all test files loaded', () => {
        // This test ensures the test suite is properly configured
        expect(true).toBe(true)
    })
})
